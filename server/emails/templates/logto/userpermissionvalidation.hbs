<!DOCTYPE html>
<html lang="{{locale}}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verify your identity</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      -webkit-font-smoothing: antialiased;
      background-color: #f5f5f5;
      margin: 0;
      padding: 20px;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
    .header {
      background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
      padding: 30px;
      text-align: center;
    }
    .header h1 {
      color: #ffffff;
      margin: 0;
      font-size: 24px;
      font-weight: 600;
    }
    .content {
      padding: 40px 30px;
    }
    .greeting {
      font-size: 16px;
      color: #374151;
      margin-bottom: 20px;
    }
    .message {
      font-size: 15px;
      color: #4b5563;
      line-height: 1.6;
      margin-bottom: 30px;
    }
    .code-container {
      background: #fef2f2;
      border-radius: 12px;
      padding: 30px;
      text-align: center;
      margin: 30px 0;
    }
    .code {
      font-size: 36px;
      font-weight: 700;
      letter-spacing: 8px;
      color: #dc2626;
      font-family: 'Courier New', monospace;
    }
    .expiry {
      font-size: 13px;
      color: #6b7280;
      margin-top: 15px;
    }
    .warning {
      font-size: 13px;
      color: #9ca3af;
      margin-top: 20px;
      padding: 15px;
      background: #fee2e2;
      border-radius: 8px;
    }
    .user-info {
      font-size: 13px;
      color: #6b7280;
      margin-top: 20px;
      padding-top: 20px;
      border-top: 1px solid #e5e7eb;
    }
    .footer {
      background-color: #f9fafb;
      padding: 25px 30px;
      text-align: center;
      font-size: 13px;
      color: #6b7280;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Identity Verification Required</h1>
    </div>

    <div class="content">
      <p class="greeting">Hello{{#if user.name}} {{user.name}}{{/if}},</p>

      <p class="message">
        For security purposes, we need to verify your identity before proceeding with this sensitive operation. Please use the following verification code:
      </p>

      <div class="code-container">
        <div class="code">{{code}}</div>
        <p class="expiry">This code will expire in 10 minutes</p>
      </div>

      {{#if application}}
      <div class="user-info">
        <p style="margin: 0;">This verification was requested for <strong>{{application.name}}</strong>.</p>
      </div>
      {{/if}}

      <p class="warning">
        If you didn't initiate this action, please secure your account immediately by changing your password and contacting support.
      </p>
    </div>

    <div class="footer">
      <p>This email was sent by {{#if application}}{{application.name}}{{else}}our service{{/if}}.</p>
      <p style="margin-top: 10px; color: #9ca3af;">&copy; {{currentYear}} All rights reserved.</p>
    </div>
  </div>
</body>
</html>
